# Exact environment the committed figures, logs, and README numbers were
# produced in (Python 3.12.13, CPU). Install with:
#
#     python -m venv .venv && ./.venv/bin/pip install -r requirements.txt -e .
#
# and reproduce.sh replays deterministically.
#
# pyproject.toml deliberately keeps LOWER BOUNDS instead (numpy>=1.24, ...):
# that is the library's compatibility contract, and CI installs from it so the
# 3.9/3.12 matrix keeps testing against current releases. This file is the
# reproduction lockfile; the two jobs are different, so the two files differ.
# Transitive dependencies (matplotlib's own) are left to pip.
numpy==2.5.0
matplotlib==3.11.0
pytest==9.1.1
mypy==2.3.0
# external-benchmark only (experiments/external_benchmark.py); never imported
# by the package or the tests, so the numpy-only CI job stays green without it.
emcee==3.1.6
